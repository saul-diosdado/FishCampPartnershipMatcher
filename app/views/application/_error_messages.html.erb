<% if object && object.errors.size > 0 %>
  <div class="alert alert-danger">
    <h4 class="alert-heading"><%= pluralize(object.errors.size, "error") %> prohibited this record from being saved</h4>
    <% object.errors.full_messages.each do |msg| %>
      <p class="mb-0"><%= msg %></p>
    <% end %>
  </div>
<% end %>
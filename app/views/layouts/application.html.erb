<!DOCTYPE html>

<html>
  <head>
    <title>FishCampPartnershipMatcher</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="#">Fish Camp</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <%= link_to("Preferences", preference_forms_path, class: "nav-link") %>
            <%= link_to("Answers", answers_path, class: "nav-link") %>
            <%= link_to("Profiles", questions_path, class: "nav-link") %>
            <%= link_to("Questions", questions_path, class: "nav-link") %>
          </ul>
        </div>
      </nav>
      <% if signed_in? %>
          Signed in as: <%= current_user.email %>
          <%= button_to "Sign out", sign_out_path, method: :delete %>
      <% end %>
    </header>
    <!--
    <div id="flash">
      <% flash.each do |key, value| %>
        <div class="flash <%= key %>"><%= value %></div>
      <% end %>
    </div>
    -->
    <%= yield %>
  </body>
</html>
